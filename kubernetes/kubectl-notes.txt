kubectl get nodes : lists every node in the cluster. The cluster is determined by Kubeconfig ~/.kube/config
get pod : gets every pod
get all : gets all pods, deployments, and services.
get configmap : get the config map
get secret : get the pods
get svc : get all services


#the image for this is busted, but assuming the image DID work, this would do something.
#next step is to create my own image that uses go and mysql or something and get that deployed.